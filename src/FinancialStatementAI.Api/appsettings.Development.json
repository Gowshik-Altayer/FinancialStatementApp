{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    "_comment": "DEV-ONLY placeholder signing key. Never use this in production - override via `dotnet user-secrets set Jwt:SigningKey \"...\"` or an environment variable instead.",
    "SigningKey": "dev-only-local-signing-key-not-for-production-use-replace-me-1234567890"
  }
}
