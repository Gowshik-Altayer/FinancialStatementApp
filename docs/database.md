# Database Documentation

Not yet populated — `AppDbContext`, entities, and the ER diagram are added in **Phase 3 (SQL
Server + EF Core)**. This file will then document the schema, key constraints, and the EF Core
migration workflow (both CLI and Visual Studio Package Manager Console).
